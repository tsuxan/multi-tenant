<%= form_with(model: @profile, local: true) do |form| %>
    <% if @profile.errors.any? %>
        <div id="error_explanation">
        <h2><%= pluralize(@profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

        <ul>
            <% @profile.errors.full_messages.each do |message| %>
            <li><%= message %></li>
            <% end %>
        </ul>
        </div>
    <% end %>
    
    <div class="field">
        <%= form.label :full_name %>
        <%= form.text_field :full_name, required: true%>
    </div>

    <div class="field">
        <%= form.label :gender %>
        <%= form.text_field :gender, required: true%>
    </div>

    <div class="field">
        <%= form.label :address %>
        <%= form.text_field :address, required: true %>
    </div>

    <div class="field">
        <%= form.label :contact_no %>
        <%= form.number_field :contact_no,required: true %>
    </div>

    <div class="field">
        <%= form.label :bio %>
        <%= form.text_area :bio,required: true %>
    </div>

    <div>
        <%= form.submit :Create %>
    </div>

<% end %>