<%= form_with(url: blog_comments_path(blog), method: :post, local: true) do |form| %>
    <p>
      <%= form.label :commenter%>
      <%= form.text_field :commenter%>
    </p>
    <p>
    </p>
    <p>
      <%= form.label :body%>
      <%= form.text_area :body%>
    </p>
    <p>
      <%= form.submit :Comment%>
    </p>
    <% end %>
        